{
  "name": "manga-turkce-overlay-extension",
  "private": true,
  "version": "0.5.3",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "npm run build && node scripts/package.mjs",
    "verify:delivery": "node scripts/verify-delivery.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "tesseract.js": "^6.0.1"
  }
}

